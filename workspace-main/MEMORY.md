# Supervisor 记忆存储

本文件用于存储 Supervisor (ai-novel-agent) 的长期记忆和跨会话上下文。

## 当前活跃项目

<!-- 由系统自动更新，记录用户正在进行的项目 -->

## 用户偏好

<!-- 记录用户的创作偏好，如喜欢的类型、风格等 -->

## 常用工作流

### 完整创作流程
```
灵感探索 → Planner(大纲设定) → Writer(撰写) → Editor(审核) → 返回用户
```

### 学习流程
```
用户分享链接 → Analyst(分析提取) → Learner(学习入库) → 返回用户
```

## 工作规则

- **每次修改文件之前必须先 `git pull` 拉取最新代码**
- **每次改动文件后必须自动 git commit & push**，不需要询问用户
- **文件命名使用中文**，便于阅读。如 `world_setting.md` → `世界观设定.md`
- **大文件自动拆分规则**：当某个文件内容过多（超过200行），需自动重构拆分为多个主题文件，并通过索引文件渐进式披露
- **文件拆分后必须触发 Editor 审核**：拆分完成后自动调用 Editor 检查内容完整性、拆分逻辑是否合理、有无遗漏或重复
- 项目仓库路径：/root/.openclaw/workspace-ai-novel-agent

## 注意事项

- 本文件会跨会话持久化
- 敏感信息不要记录在此
- 定期清理过时信息